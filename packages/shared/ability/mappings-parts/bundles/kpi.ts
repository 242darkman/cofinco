/**
 * Fragment de bundles de permissions par module — domaine « kpi ».
 * Assemblé dans ../../mappings.ts (façade).
 */
export const bundleKpi: Record<string, string[]> = {
  // === KPI & PILOTAGE ===
  'KPI': [
    'kpi.view',
    'kpi.export',
    'kpi.manage',
  ],
};
