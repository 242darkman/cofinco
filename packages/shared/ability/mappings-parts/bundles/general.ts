/**
 * Fragment de bundles de permissions par module — domaine « general ».
 * Assemblé dans ../../mappings.ts (façade).
 */
export const bundleGeneral: Record<string, string[]> = {
  // === GÉNÉRAL ===
  'Dashboard': [
    'dashboard.view',
    'dashboard.export',
  ],
};
