apiVersion: 1

providers:
  - name: 'MICROFLEX Dashboards'
    orgId: 1
    folder: 'MICROFLEX'
    folderUid: 'microflex'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
