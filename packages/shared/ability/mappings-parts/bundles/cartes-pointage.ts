/**
 * Fragment de bundles de permissions par module — domaine « cartes-pointage ».
 * Assemblé dans ../../mappings.ts (façade).
 */
export const bundleCartesPointage: Record<string, string[]> = {
  // === FINANCE - CARTES DE POINTAGE ===
  'Cartes de Pointage': [
    'cartespointage.view',
    'cartespointage.create',
    'cartespointage.deposit',
    'cartespointage.withdraw',
    'cartespointage.manage',
  ],
};
